# 资源维护说明

## models-v1 发布流程

- 实际仓库为 `Xiaowu7z/avatar-resources`，固定标签为 `models-v1`，根目录 `catalog-v1.json` 已配置正式链接。
- 工作流 `publish-models.yml` 从 `manifests/upstream-sources.json` 的官方源获取模型。ASR 固定到提交，KWS 固定原始归档 SHA-256，所有内部文件再次校验。
- `tools/pack_resources.py` 与配套 App 的打包算法一致。主分支只包含小文件；不要提交 `.part`、APK、模型权重、私钥、API 凭据或诊断日志。
- 验证 ZIP 完整哈希及许可文件后，先创建草稿、上传两份 ZIP、许可、归档清单、目录快照，核对完整资产列表和大小后公开。
- 固定公开版本不覆盖。不论改变压缩参数、权重还是关键词，都必须发布新文件名、新清单与新标签，并检查 App 兼容性。
- 如果同一提交上传失败，可重跑该工作流；如果已有草稿属于另一提交，脚本会停止，需先审阅该草稿。已公开 Release 的重跑也会停止，避免原地替换。

## 当前两个版本的边界

- `asr/paraformer/manifest.json` 在 APK 中约束 Paraformer 的编码器、解码器与 tokens 文件。
- `kws/manifest.json` 在 APK 中约束 Zipformer 唤醒文件与“贾维斯”关键词文件。
- `manifests/` 是这些可信清单的配套副本；发布者不能只改目录字段便要求旧 APK 接受其他权重。
- 同一推理引擎版本不等于模型相互兼容。增加模型还可能需要新的前处理、采样率、tokens、解码器、CPU/内存策略或 JNI 接口。
- TTS 当前使用 Android 已安装引擎列表。微软在线语音需要单独实现授权配置与联网合成；微软离线模型需要合法取得对应授权。这里不声明存在任何未提供的声音包。

## 新增资源的顺序

1. 确定资源来源与许可，记录模型身份、版本、配套运行时和目标 App。
2. 在 App 中实现并验证适配器，加入允许的文件路径、大小、哈希、资源切换与诊断信息。
3. 生成新资源包和内部文件清单，新增目录版本记录，明确已支持的 App 版本。旧版本条目和旧 Release 文件保留。
4. 运行发布校验，创建新的固定标签，上传完整资源及许可；再提交稳定目录入口。

本模板脚本只接受当前两种已适配资源，不作为任意模型的发布平台。增加资源类型时，需同步扩展目录结构检查和 App 适配；不要为了让校验通过而删掉哈希或放开任意文件路径。

## 完整性与可信来源

SHA-256 用于发现损坏或资源被替换，不等同于发布者数字签名。当前 App 以随 APK 分发的清单为信任依据。未来若支持远程清单更新，应另行设计签名验证和版本回退规则，不能仅凭一个可变 URL 和同处下载的哈希来决定执行任意资源。

`tools/catalog.py validate` 只做本地校验；不请求 GitHub、不检查发布状态、不修改手机内容。Python 3 标准库即可运行。
